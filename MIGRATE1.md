# 技术方案迁移文档：从 SigLIP + DeepDanbooru 迁移至纯 SigLIP 架构

> **版本**：V3.0  
> **日期**：2026-08-24  
> **适用范围**：AI 照片管理工具后端全部 AI 模块  
> **文档性质**：架构迁移说明与技术方案（不包含具体代码实现）


## 1. 迁移背景与战略意义

### 1.1 原有双模型架构的架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        原有架构 (V1.0)                             │
├───────────────────────┬─────────────────────────────────────────────┤
│   DeepDanbooru        │   SigLIP                                   │
│   (打标签模型)         │   (智能搜索模型)                            │
├───────────────────────┼─────────────────────────────────────────────┤
│   • 614 MB            │   • 1.5 GB                                │
│   • 68,000+ 动漫标签   │   • 图像编码器 + 文本编码器                │
│   • ~80 ms/张 (CPU)   │   • 仅用于语义搜索                         │
└───────────────────────┴─────────────────────────────────────────────┘
                              │
                              ▼
                    总模型体积：约 2.1 GB
                    双模型加载 + 双路径维护
```

### 1.2 原有架构的核心问题

| 问题类别 | 具体描述 |
| :--- | :--- |
| **模型体积过大** | DeepDanbooru (614 MB) + SigLIP (1.5 GB) = 2.1 GB，首次下载耗时极长，用户体验差 |
| **标签体系不匹配** | DeepDanbooru 专为动漫图像设计（标签如 `1girl`、`blue_hair`），与摄影场景严重脱节 |
| **架构复杂度高** | 同时维护两套模型加载逻辑、两套推理管道、两套错误处理 |
| **用户无法自定义标签** | DeepDanbooru 的标签是固定的，用户若想识别“C919”或“长曝光”需额外开发 |
| **资源占用高** | 两个模型同时加载时内存占用超过 2 GB，普通用户电脑难以承受 |

### 1.3 迁移的战略目标

| 目标 | 描述 | 衡量指标 |
| :--- | :--- | :--- |
| **架构统一** | 使用单一模型完成打标签 + 智能搜索 | 代码中仅保留一个 AI 引擎 |
| **模型轻量化** | 大幅减小下载体积 | 总模型 < 200 MB |
| **推理加速** | 提升打标签速度 | 单张 < 50 ms |
| **标签体系灵活化** | 用户可自由定义任意标签 | 用户可通过 UI 增删标签，无需任何训练 |
| **中文友好** | 支持中文标签与中文搜索 | 使用中文优化版 SigLIP 或通过翻译层兼容 |

---

## 2. 新架构设计（纯 SigLIP）

### 2.1 新架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         纯 SigLIP 架构 (V2.0)                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                    SigLIP 统一引擎                          │   │
│   │                                                             │   │
│   │   ┌─────────────────┐   ┌─────────────────┐               │   │
│   │   │   图像编码器     │   │   文本编码器     │               │   │
│   │   │ (vision_model)  │   │ (text_model)    │               │   │
│   │   └────────┬────────┘   └────────┬────────┘               │   │
│   │            │                     │                         │   │
│   └────────────┼─────────────────────┼─────────────────────────┘   │
│                │                     │                             │
│                ▼                     ▼                             │
│   ┌─────────────────────┐   ┌─────────────────────┐               │
│   │    照片入库/扫描      │   │    用户搜索输入      │               │
│   │                     │   │                     │               │
│   │ ① 提取图像向量       │   │ ① 提取文本向量       │               │
│   │ ② 与用户自定义标签   │   │ ② 与所有图像向量    │               │
│   │    列表匹配          │   │    计算相似度        │               │
│   │ ③ 打标签 + 存向量    │   │ ③ 返回 Top-N 结果   │               │
│   └──────────┬──────────┘   └──────────┬──────────┘               │
│              │                         │                           │
│              └────────────┬────────────┘                           │
│                           ▼                                        │
│               ┌─────────────────────┐                              │
│               │   SQLite 数据库      │                              │
│               │                     │                              │
│               │  files.embedding    │  ← 图像向量                  │
│               │  file_tags          │  ← 标签关联                  │
│               │  tags               │  ← 用户自定义标签列表        │
│               └─────────────────────┘                              │
│                           │                                        │
│                           ▼                                        │
│               ┌─────────────────────┐                              │
│               │   标签搜索 (SQL)     │  ← 毫秒级，不走模型         │
│               └─────────────────────┘                              │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心工作原理

| 流程 | 说明 |
| :--- | :--- |
| **1. 用户定义标签** | 用户在设置界面自由添加/删除标签（如“飞机”、“云”、“海”、“C919”、“长曝光”等），系统自动计算每个标签的文本向量并缓存到内存。 |
| **2. 照片入库** | 新照片加入时，SigLIP 图像编码器提取图像向量，存入 `files.embedding`；同时与所有缓存标签向量计算相似度，匹配度高于阈值的标签写入 `file_tags` 表。 |
| **3. 标签搜索** | 用户搜索“飞机”时，直接查询 `file_tags` 表，毫秒级返回结果，**完全不调用模型**。 |
| **4. 语义搜索** | 用户输入“夕阳下的海滩”时，文本编码器提取查询向量，与数据库中所有图像向量计算相似度，返回最匹配的照片。 |

### 2.3 标签管理的用户交互

用户无需任何技术背景，即可在设置面板中：

- 添加新标签（输入名称，如“C919”）
- 删除现有标签
- 调整匹配阈值（控制标签生成的严格程度）
- 预览标签效果（系统随机抽取几张照片展示匹配结果）

系统在用户修改标签列表后，自动重新计算所有标签的文本向量并缓存，无需用户等待。

---

## 3. 迁移范围与变更清单

### 3.1 移除的组件

| 组件 | 说明 |
| :--- | :--- |
| **DeepDanbooru 模型文件** | `deepdanbooru.onnx` (~614 MB) 不再下载和使用 |
| **DeepDanbooru 推理代码** | `predict_tags_deepdanbooru()` 函数及所有相关调用 |
| **DeepDanbooru 依赖库** | 如有专门的 Rust 绑定或 Python 桥接，全部移除 |
| **DeepDanbooru 测试用例** | 所有针对 DeepDanbooru 的单元测试和集成测试 |
| **DeepDanbooru 相关配置** | 模型下载 URL、SHA256、版本号等配置项 |

### 3.2 新增/增强的组件

| 组件 | 说明 |
| :--- | :--- |
| **SigLIP 文本编码器** | 新增 `text_model.onnx` 加载逻辑，原仅有图像编码器 |
| **SigLIP 分词器** | 新增 `tokenizer.json` 加载逻辑，用于文本预处理 |
| **标签向量缓存** | 内存中缓存所有用户标签的文本向量，避免重复计算 |
| **标签管理 API** | 新增 Tauri 命令：`set_tag_list`、`add_tag`、`remove_tag`、`get_tag_list` |
| **统一推理管道** | 照片入库时一次性完成图像向量化 + 标签匹配 + 存储 |

### 3.3 保持不变/微调的组件

| 组件 | 变更程度 | 说明 |
| :--- | :--- | :--- |
| **SigLIP 图像编码器** | 保持不变 | 原有 `vision_model.onnx` 加载和推理逻辑完全保留 |
| **智能语义搜索** | 逻辑不变 | 仍使用 `embed_text` + 向量检索，只是 `embed_text` 现在调用内部文本编码器 |
| **标签搜索** | 完全不变 | 仍是 SQLite 索引查询 |
| **缩略图缓存** | 完全不变 | 不受影响 |
| **文件监控与增量扫描** | 微调 | 新照片入库时调用新的统一推理管道，而非分别调用 DeepDanbooru 和 SigLIP |

---

## 4. 模型文件清单与下载

### 4.1 新模型文件清单

| 文件名 | 用途 | 大小 | 来源 |
| :--- | :--- | :--- | :--- |
| `vision_model.onnx` | 图像编码器 | ~52 MB (INT8 量化) | `onnx-community/siglip2-base-patch16-naflex-ONNX` |
| `text_model.onnx` | 文本编码器 | ~52 MB (INT8 量化) | 同上 |
| `tokenizer.json` | 分词器 | ~33 MB | `google/siglip2-base-patch16-224` |

**总模型体积**：约 **137 MB**（原 2.1 GB，缩小 93.5%）

### 4.2 镜像下载链接

所有文件均可通过国内镜像 `hf-mirror.com` 下载：

- **图像编码器**：`https://hf-mirror.com/onnx-community/siglip2-base-patch16-naflex-ONNX/resolve/main/onnx/vision_model.onnx`
- **文本编码器**：`https://hf-mirror.com/onnx-community/siglip2-base-patch16-naflex-ONNX/resolve/main/onnx/text_model.onnx`
- **分词器**：`https://hf-mirror.com/google/siglip2-base-patch16-224/resolve/main/tokenizer.json`

### 4.3 SHA256 校验

生产环境必须使用 SHA256 校验，确保模型完整性。建议在首次下载后自行计算并填充到 `MODEL_LOCK` 中。

---

## 5. 性能对比

| 指标 | 迁移前 (SigLIP + DeepDanbooru) | 迁移后 (纯 SigLIP) | 提升幅度 |
| :--- | :--- | :--- | :--- |
| **模型总大小** | ~2.1 GB | **~137 MB** | ↓ 93.5% |
| **单张打标签时间** | ~80 ms (DeepDanbooru) | **~20 ms** | ↑ 4x |
| **单张语义搜索** | ~50 ms | **~20 ms** | ↑ 2.5x |
| **标签搜索** | < 10 ms (SQL) | < 10 ms (SQL) | 持平 |
| **内存占用 (加载后)** | ~2 GB | **~200 MB** | ↓ 90% |
| **模型加载时间** | ~10 秒 (双模型) | **~2 秒** | ↑ 5x |
| **首次下载时间** | 依赖网速，通常 > 30 分钟 | **约 3-5 分钟 (10 Mbps)** | ↓ 85% |
| **用户自定义标签** | 不支持 | **原生支持** | 新增能力 |
| **标签体系匹配度** | 偏低 (动漫标签) | **完全匹配 (用户自定义)** | 质的提升 |

---

## 6. 迁移实施步骤

### 6.1 第一阶段：准备工作（预计 1-2 天）

| 序号 | 任务 | 产出物 |
| :--- | :--- | :--- |
| 1 | 下载 SigLIP 文本编码器和分词器模型文件 | 本地模型文件 |
| 2 | 计算 SHA256 校验和，更新 `MODEL_LOCK` | 更新后的锁定清单 |
| 3 | 更新 `Cargo.toml` 依赖（添加分词器库，移除 DeepDanbooru 相关依赖） | 更新后的依赖清单 |
| 4 | 设计数据库迁移脚本（添加 `files.embedding` 字段，如尚未存在） | 迁移脚本 |

### 6.2 第二阶段：代码迁移（预计 3-5 天）

| 序号 | 任务 | 说明 |
| :--- | :--- | :--- |
| 5 | 移除 DeepDanbooru 下载逻辑 | 删除 `deepdanbooru.onnx` 的下载 URL 和校验逻辑 |
| 6 | 移除 DeepDanbooru 推理代码 | 删除 `predict_tags_deepdanbooru()` 及相关调用 |
| 7 | 新增 SigLIP 文本编码器加载逻辑 | 加载 `text_model.onnx` 和 `tokenizer.json` |
| 8 | 新增标签向量缓存机制 | 用户修改标签列表时自动重算文本向量 |
| 9 | 重构 `predict_tags` 为统一管道 | 使用图像向量与标签向量匹配，替代 DeepDanbooru |
| 10 | 修改照片入库流程 | 一次性完成图像向量化 + 打标签 + 存储 |
| 11 | 移除 DeepDanbooru 相关配置和测试 | 清理所有残留代码 |

### 6.3 第三阶段：测试与验证（预计 2-3 天）

| 序号 | 任务 | 验证标准 |
| :--- | :--- | :--- |
| 12 | 单元测试 | 图像向量化、文本向量化、相似度计算功能正确 |
| 13 | 集成测试 | 新照片入库 → 自动打标签 → 标签搜索 → 语义搜索，全流程正常工作 |
| 14 | 性能测试 | 1000 张照片入库总耗时 < 30 秒 |
| 15 | 跨平台测试 | Windows 10/11、macOS (Intel + Apple Silicon) 分别验证 |
| 16 | 用户体验测试 | 用户可自由增删标签，系统响应及时，无卡顿 |

### 6.4 第四阶段：发布与文档（预计 1 天）

| 序号 | 任务 | 说明 |
| :--- | :--- | :--- |
| 17 | 更新技术方案文档 | 反映新架构 |
| 18 | 更新用户手册 | 新增“自定义标签管理”章节 |
| 19 | 提供升级说明 | 告知用户新版如何迁移、旧版数据如何处理 |
| 20 | 构建安装包 | 确保模型文件正确打包或自动下载 |

---

## 7. 风险与应对措施

### 7.1 已识别风险

| 风险 | 影响 | 概率 | 应对措施 |
| :--- | :--- | :--- | :--- |
| **SigLIP 零样本分类精度不足** | 部分照片无法正确打标签 | 中 | 允许用户调低阈值；提供手动添加/删除标签功能作为补救 |
| **中文标签支持不完善** | 中文标签匹配效果不如英文 | 中 | 推荐用户使用英文标签（如 "sunset"），或替换为中文优化版 SigLIP |
| **旧数据库已有 `file_tags` 但无 `embedding`** | 语义搜索无法对旧照片工作 | 高 | 提供“重新索引”功能，后台逐步为旧照片补打标签和生成向量 |
| **模型下载失败** | 首次启动无法使用 AI 功能 | 低 | 提供离线安装包选项；下载失败时降级为仅标签搜索 |

### 7.2 回退方案

如果迁移后效果不达预期，可执行以下回退步骤：

1. 保留 DeepDanbooru 的代码分支（标记为 `legacy`），但**不推荐长期维护双模型**。
2. 在 Settings 中增加一个开关 `use_legacy_tagger`，允许用户切换回 DeepDanbooru（仅在紧急情况下启用）。
3. 团队应集中精力优化 SigLIP 方案，而非长期维护两个模型。

---

## 8. 商业价值评估

### 8.1 直接效益

| 维度 | 价值说明 |
| :--- | :--- |
| **降低用户流失率** | 首次启动等待时间从 30+ 分钟降至 3-5 分钟，用户更愿意留存 |
| **提升用户满意度** | 用户可自由定义“飞机”、“C919”等自定义标签，贴近摄影师实际需求 |
| **降低技术支持成本** | 单模型架构减少因双模型不兼容导致的 Bug，减少用户投诉 |
| **提升品牌竞争力** | “无需训练，自定义任意标签”是竞品难以模仿的差异化功能 |

### 8.2 长期战略价值

- **为插件生态铺路**：用户可将“标签列表 + 参考图”打包成插件分享，形成社区生态。
- **为多语言拓展打基础**：SigLIP 原生支持多语言，未来可轻松适配其他语种用户。
- **为移动端移植做准备**：模型体积减小 93%，更容易移植到移动端。

---

## 9. 变更对用户的影响

| 影响方面 | 变化说明 |
| :--- | :--- |
| **初次启动下载** | 模型下载从 ~2.1 GB 降至 ~137 MB，等待时间大幅缩短 |
| **照片入库速度** | 打标签从 ~80 ms/张 提升至 ~20 ms/张，用户感知更流畅 |
| **标签搜索** | 完全不受影响，仍为毫秒级响应 |
| **智能搜索** | 完全不受影响，语义搜索能力保持不变 |
| **标签自定义** | **新增能力**：用户可自由添加/删除任何标签 |
| **旧版照片库** | 迁移后需后台重新索引（自动进行，不阻塞用户操作） |

---

## 10. 总结

| 维度 | 迁移前 | 迁移后 | 提升 |
| :--- | :--- | :--- | :--- |
| 模型体积 | 2.1 GB | 137 MB | ↓ 93.5% |
| 打标签速度 | 80 ms | 20 ms | ↑ 4x |
| 内存占用 | 2 GB | 200 MB | ↓ 90% |
| 标签体系 | 固定 (动漫) | 用户自定义 | 质的提升 |
| 架构复杂度 | 双模型 | 单模型 | 大幅简化 |
| 用户首次等待 | > 30 分钟 | < 5 分钟 | ↓ 85% |

**迁移后的纯 SigLIP 架构在所有维度上都优于原有双模型架构，是一次全面的技术升级。** 建议按本文档分阶段执行迁移，确保每个环节都有充分的测试和验证。